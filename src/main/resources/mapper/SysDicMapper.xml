<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tansci.mapper.SysDicMapper">

    <select id="getDicChildrens" parameterType="integer" resultType="com.tansci.domain.SysDic">
        WITH RECURSIVE t AS (
            SELECT * FROM sys_dic WHERE id = #{id}
            union ALL
            SELECT sys_dic.* FROM sys_dic, t
            WHERE sys_dic.parent_id = t.id
        )
        SELECT t.* FROM t ORDER BY id
    </select>

</mapper>